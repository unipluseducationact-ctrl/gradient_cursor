# Gradient on Map — Interactive

An interactive, single-file HTML demo that visualizes map contours and a gradient triangle (rise/run, \( \tan\theta \), etc.).

## Run locally

- Open `index.html` in your browser.

## Notes

- Uses GSAP via CDN for animation.

